import styled from 'styled-components';

export const App = styled.div`
  min-height: 100vh;
  min-height: 100dvh;
  padding-top: 7rem;
  display: grid;
  grid-template-rows: auto 1fr auto;
`;
