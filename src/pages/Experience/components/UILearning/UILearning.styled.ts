import styled from 'styled-components';
import { fade_slide_in_left, section_style } from '../../../../styles';

export const UILearning = styled.section`
  ${section_style};
  ${fade_slide_in_left};
`;
