import styled from 'styled-components';
import { fade_slide_in_left } from '../../../../styles';

export const MyPicture = styled.section`
  img {
    border-radius: 50%;
  }
  ${fade_slide_in_left};
`;
